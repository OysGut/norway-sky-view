// LOCKED: engine code — modify only on explicit engine tasks.
// Future purpose: pure math for the Bent World transform (flat zone, bend radius, remap curve).

export const BEND_MATH_PLACEHOLDER = true;
